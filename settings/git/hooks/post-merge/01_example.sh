# uncommenting the below is an example of the hook
# echo "something just got merged!/pulled"

# once `nix-shell` is run one time, all hook will be generated
# and can be edited without needing to run `nix-shell` again