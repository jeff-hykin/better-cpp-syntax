static void a(
                           a    a,
                           a a,
                           a a,
                           a a,
                           a a,
                           bool                       a,
                           bool                       a,
                           bool                       a,
                           bool                       a)

{
  a a;


  a(a, a,
                                 a,
                                 a,
                                 a,
                                 a,
                                 a,
                                 a,
                                 a);
  a->a(&a);
  for (auto a : a) {
    auto &a = a.a;
    auto a = a.a;

    if (a && a) {
      switch (a) {
        case a:
          a->a(a->a());
          break;
        case a:
          a->a(a->a());
          break;
        case a:
          a->a(a->a());
          break;
        case a:
          a->a(a->a());
          break;
      }  
    }
    a(a, a,
                                   a,
                                   a,
                                   a,
                                   a,
                                   a,
                                   a,
                                   a);
  } 
}  

static void a(
                      a                    a,
                      a           a,
                      a a,
                      a                                 a)

{
  a a;
  a a =0;
                               a->a()->a();

  a(a->a());
  a->a(a);
  a->a(a);
  a(a, a, a,
                                 a,
                                 a);
  a->a(&a);
  for (auto a: a) {
    a(a->a(), a, a,
                                                                   a);
  }
}