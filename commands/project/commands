#!/usr/bin/env bash
    echo "project commands:"
    tree "$PROJECTR_COMMANDS_FOLDER/project" -C --dirsfirst -A -F --noreport | sed 's/^/    /' | sed "1d"
    echo 
    echo "examples:"
    echo "    project clean"
    echo "    _ clean"
    
